events {}

http {
  server {
    listen 80;

    # FRONTEND
    location / {
      root /usr/share/nginx/html;
      index index.html;
    }

    # BACKEND API
    location /api/ {
      proxy_pass http://backend:8080;
    }
  }
}